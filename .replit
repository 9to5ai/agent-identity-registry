run = "pip install -e . && uvicorn src.agent_registry.main:app --host 0.0.0.0 --port 8080"
language = "python3"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "pip install -e . && uvicorn src.agent_registry.main:app --host 0.0.0.0 --port 8080"]

[[ports]]
localPort = 8080
externalPort = 80
